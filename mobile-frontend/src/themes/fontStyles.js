import {ms,mvs} from 'react-native-size-matters';

// cfs: common font styles

const cfs = {
  bannerDesc: {
    fontSize: 12,
    fontWeight: '300',
  },
  bannerTitle: {
    fontSize: 18,
    fontWeight: '500', 
  },
};

export default cfs ;

// const fontStyles: {
//   regularsize12weight400: {
//   },
// };